let name; // Result Undefined 
console.log(name);

function add(name1, name2) {
    console.log(num1 + num2);
}
const result = add(23,34);  // if we are put only one value it's also undefined.
console.log ( result);  // result  undefined  because we are not return anything in we are just type return then also it's undefined.

let fun = undefined; // when variable value undefined result also undefined
console.log(fun);

